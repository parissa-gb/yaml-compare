# yaml-compare
Compares two YAML files with semantic diff. Supports Kubernetes ConfigMaps, Helm templates, and nested YAML structures. Generates HTML reports with Light/Night/High-Contrast themes. Provides clear visualization of changed values, additions, and removals.
